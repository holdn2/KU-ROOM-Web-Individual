export interface RankListType {
  name: string[];
  sharingCount: number;
}

export interface LocationTotalRankType {
  nickname: string[];
  ranking: number;
  sharingCount: number;
  isSelf: boolean;
}
